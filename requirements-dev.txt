# Development dependencies for TORI

# Build tools
maturin>=1.0

# Core dependencies
networkx>=3.0
pydantic>=2.6
pydantic-settings>=2.0
scipy>=1.12
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7

# Web framework
requests>=2.31
psutil>=5.9
uvicorn>=0.30
fastapi>=0.111

# Testing
pytest>=7.4
pytest-asyncio>=0.21

# ML dependencies for Saigon LSTM
torch>=2.3
transformers>=4.40

# Async file operations
aiofiles>=23.0

# Diff computation for Phase 6
deepdiff>=6.7

# SSE streaming for upload progress (CRITICAL)
sse-starlette>=1.8,<2

# PDF processing (CRITICAL)
pypdf2>=3

# Test dependencies
reportlab>=3.6

# Document extraction dependencies
yake>=0.4
keybert
sentence-transformers
spacy>=3.7

# Entropy-based concept pruning
# entropy-prune==0.2.1  # Commented out - use TORI_DISABLE_ENTROPY_PRUNE=1 instead

# OCR support for scanned PDFs (optional)
pytesseract
pdf2image
pillow

# Optional: Topological data analysis (may fail on Windows)
# gudhi>=3

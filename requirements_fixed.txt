# CRITICAL: These are the CORRECT versions that work together
# NumPy MUST be 1.26.4 or lower - NOT 2.x!
# Generated: 2025-08-06

# Core scientific packages - NumPy 1.26.4 is REQUIRED
numpy==1.26.4
scipy==1.14.1
pandas==2.2.3
matplotlib==3.9.2
scikit-learn==1.5.2
numba==0.60.0

# spaCy and dependencies - these versions are compatible with NumPy 1.26.4
Cython==3.0.11
cymem==2.0.8
murmurhash==1.0.10
preshed==3.0.9
thinc==8.2.4
spacy==3.7.5
spacy-legacy==3.0.12
spacy-loggers==1.0.5

# Language processing
catalogue==2.0.10
srsly==2.4.8
wasabi==1.1.3
typer==0.13.1
pydantic==2.10.4
jinja2==3.1.4
langcodes==3.5.0

# ML/DL packages
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Additional utilities
confection==0.1.5
cloudpathlib==0.20.0
weasel==0.4.1
smart-open==7.1.0

# IMPORTANT NOTES:
# 1. DO NOT upgrade NumPy to 2.x - it will break spaCy!
# 2. Install in this order: numpy first, then scipy/pandas, then spaCy
# 3. Use --no-cache-dir flag when installing to avoid cached incompatible versions
# 4. If you get ABI errors, uninstall everything and reinstall in order